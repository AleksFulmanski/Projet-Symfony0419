{% extends 'base.html.twig' %}

{% block body %}


    <h1>Best Projects Ever. Have fun!</h1>

    {#{% for prod in projects %}#}
        <br>
        <div class="card" style="width: 18rem;">
            <div class="card-body">
                <h5 class="card-title">{#{ prod.name | upper }#}</h5>
                <p class="card-text">{#{ prod.description  | slice(0, 80) | raw('html') }#}</p>
                <a href="{#{ path('app_project_show', {slug : prod.slug}) }#}" class="btn btn-primary">Détails</a>
            </div>
        </div>
        <br>
    {#{% endfor %}#}

{% endblock body %}